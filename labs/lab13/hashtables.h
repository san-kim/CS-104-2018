#ifndef HASHTABLES_H
#define HASHTABLES_H

#include <vector>
#include <string>
#include <utility>

bool isAnagram(std::string s, std::string t);
std::pair<int, int> twoSum(std::vector<int> input, int target);

#endif